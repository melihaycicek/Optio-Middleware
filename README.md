# Optio-Middleware
A local AI middleware that enforces strict JSON structure on natural language commands. QLoRA fine-tuned on consumer hardware (RTX 4060) to bridge the gap between chaotic user inputs and backend logic.
